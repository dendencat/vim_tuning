# ===== Deploy destinations (必要に応じて変更) =====
DEPLOY_VIMRC="${HOME}/.vimrc"
DEPLOY_VIMRC_CORE="${HOME}/.vim/vimrc_core.vim"
DEPLOY_INITVIM="${HOME}/.config/nvim/init.vim"

# 差分表示に使うページャ
PAGER_CMD="${PAGER:-less -R}"

# 上書き前バックアップを取るか
ENABLE_BACKUP="1"

# バックアップ保存先（同ディレクトリ。変更可）
# 例: ~/.local/share/dotfile_backups にまとめたい場合は変更
# 空ならデプロイ先ファイルと同じディレクトリに保存
BACKUP_DIR=""

